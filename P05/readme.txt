The P05_ctrl script will load the entire schema.

To execute rows from the simulation table: SQL> exec cmd(<begin row>, <end row>);

To reserve flights: SQL> exec reserve.reserve_em(<FID 1> [<FID 2>] [<FID 3>] <CUST ID>);